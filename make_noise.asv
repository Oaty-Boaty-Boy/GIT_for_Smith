fs= 44100

noise = randn (1,fs * noisedur);
noisedur = 2;

%sound (noise,fs)
plot (noise)
b=Hd.Numerator;
Yoink=filter(Hd,noise);
sound (Yoink,fs);
y=conv(sig_pluck,Ir2(:,1));



a=1;
newnoise1 = conv( noise)
newnoise2 = 