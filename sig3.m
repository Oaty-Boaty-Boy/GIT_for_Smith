Fs = 10000;    % Sample rate (Hz)
dur = 0.05;      % Duration in seconds
freq = 1000;     % Frequency (Hz)
theta = 0;   % Phase offset (degrees)
amp = 0.5;      % Amplitude

% Call the function to generate and plot the sine wave
SineWave(amp, freq, Fs, dur, theta);